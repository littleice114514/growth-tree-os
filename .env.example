# growth-tree-os environment example
#
# 当前 V1 不需要业务环境变量即可运行。
# Electron/Vite 开发模式会自动注入 ELECTRON_RENDERER_URL，不需要手动填写。
#
# 如后续接入云服务或外部 API，请只在本地 .env / .env.local 中填写真实密钥。

# ── Wealth Market Data (Finnhub) ──
# 从 https://finnhub.io 注册获取免费 API Key
# Key 仅在 Electron main process 中读取，不会暴露到 renderer 前端
FINNHUB_API_KEY=
